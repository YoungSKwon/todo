# TODO

간단한 할 일 관리 웹 앱입니다. 별도의 빌드 과정 없이 `index.html`을 브라우저에서 열면 바로 사용할 수 있습니다.

## 기능

- 할 일 추가 / 완료 토글 / 삭제
- 더블클릭으로 텍스트 수정 (빈 값으로 지우면 항목 삭제)
- 전체 / 진행중 / 완료 필터
- 완료 항목 일괄 삭제
- `localStorage`에 자동 저장 (새로고침해도 유지)

## 실행 방법

`index.html` 파일을 브라우저에서 열기만 하면 됩니다.

## 구성

- `index.html` — 마크업
- `style.css` — 스타일
- `app.js` — 할 일 상태 관리 및 렌더링 로직
